# Application source

`src/` is intentionally empty in Repository Pack v0.1. Майбутній код пишеться на **Flutter + Dart**.

First implementation slice should prove:
- navigation;
- YAML/compiled JSON catalog;
- Exercise Engine;
- state machine;
- audio synchronization;
- tracking;
- localization;
- Pro feature flag behavior.


## Flutter note

Після ініціалізації Flutter-проєкту основний код буде жити в `lib/`.
Папка `src/` у цьому pack є лише placeholder і може бути видалена/замінена стандартною Flutter-структурою.
